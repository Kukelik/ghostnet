# Ghost Net Fishing
## Voraussetzungen
- Java 21
- MySQL (z.B. via XAMPP)
- Maven

## Setup
1. Datenbank `ghostnet` in MySQL anlegen
2. In `application.yml` Datenbankzugangsdaten anpassen
3. Anwendung starten über IntelliJ oder `mvn spring-boot:run`
4. Browser öffnen: `http://localhost:8080`
